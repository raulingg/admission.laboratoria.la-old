import React from 'react';
import logo from '../style/logo.svg';


const TopBar = () => (
  <header>
    <img src={logo} style={{ width: 200, margin: '7px 0' }} alt="Laboratoria - Código que transforma" />
  </header>
);


export default TopBar;
